---
name: he-design
version: 1.0.0
author: liuhe37186
repository: https://github.com/liuhe37186/he-design.git
description: 只要用户要你设计前端网页（Web Design）、Web应用、组件样式、交互界面、落地页、首屏、专题页、功能介绍页、设计规范，或要求“做得更高级、更有秩序、更像发布页/编辑页/说明页”，就应优先使用这个技能；当任务涉及网页视觉层级、响应式版式骨架、留白、素材职责、Web交互状态、设计到代码的规范翻译时，也要触发。它专精于现代 Web 页面与交互设计，不用于纯海报出图、纯品牌手册搬运、纯后端逻辑或无界面的数据处理。
---

# He Design

## 作用

将高级“审美系统”抽取为通用的 Web 页面设计规范，服务于现代 Web 网页、前端组件和交互界面的设计与代码实现。

这个技能的目标不是堆更多装饰，而是帮助宿主智能体先建立 Web 页面的主次关系、响应式骨架、阅读节奏和品牌气质，再把这些判断翻译成符合现代 Web 标准的可开发页面结构与样式约束。

## 何时使用

- 用户要做 Web 页面视觉设计、首屏、落地页、专题页、功能介绍页
- 用户要做 Web 交互界面、组件样式、信息型网页或 Web UI 改版
- 用户要求网页“更高级”“更有秩序”“更像品牌发布页/信息说明页”
- 用户给的是文案、模块、页面目标，希望你整理成 Web 设计规范或可开发界面
- 用户已经有页面代码，但需要统一层级、留白、响应式骨架和 Web 交互状态

## 不适用

- 纯海报生成或社媒图片输出（画板思维不适用 Web）
- 只做品牌历史、吉祥物、logo 手册整理
- 只处理接口、数据库、脚本、后端逻辑
- 用户明确要求套用已有强品牌设计系统时，应优先用对应品牌技能

## 技能资源

- `references/aesthetic-system.md`
- `references/page-archetypes.md`
- `references/interaction-rules.md`
- `references/output-contract.md`
- `references/review-checklist.md`
- `evals/evals.json`

## 核心哲学

1. **标题主导**：中心命题必须先被看见，符合 F 型或 Z 型网页阅读视线。
2. **结构先于素材**：去掉图片后，依靠排版与留白，网页仍应成立。
3. **节奏依赖对比**：大块、小块、密区、疏区必须分明，利用流式布局控制信息密度。
4. **气质必须统一**：不能把多套视觉语言拼在一起，UI 控件应有统一的 Design Token。
5. **留白优先**：内容过满时先删减，不靠缩小字号硬塞，保证屏幕上的呼吸感。
6. **Web 原生与流式**：设计基于浏览器特性，适应视口（Viewport）变化，而非固定尺寸的死画板。
7. **语义与可访问性**：视觉层级应与 HTML 语义标签（h1-h6）匹配，保障基础的 Web 体验。

## 默认工作流

1. 先判断任务属于哪类 Web 表达目标：
   - `release`：发布、升级、能力介绍、品牌型网页
   - `explain`：流程说明、规则摘要、知识导览、文档型网页
   - `campaign`：活动传播、情绪表达、强视觉专题页
   - `product_ui`：后台、工作台、表单流、管理界面
2. 再判断信息密度：
   - `low`
   - `medium`
   - `high`
3. 再判断终端与响应式策略：
   - `desktop_first` (桌面优先，强表现力)
   - `mobile_first` (移动优先，轻量级)
   - `fully_responsive` (全端自适应响应)
   - `fluid_compact` (流式紧凑卡片/组件)
4. 再判断是否真的需要视觉素材：
   - `none`
   - `mood`
   - `recognition`
   - `proof`
5. 再选择最接近的 Web 页面原型，不要直接从零散元素开始拼：
   - `hero_release`
   - `feature_grid`
   - `editorial_explain`
   - `dashboard_polish`
   - `form_workspace`
6. 按需读取参考文件，先给出设计方向，再落到页面结构与 Web 交互规则
7. 若用户要代码，先输出设计说明，再给组件拆分、CSS Token、语义化标签建议和状态规则
8. 交付前必须根据自检清单复查层级、留白、交互、响应式和素材边界

## 信息不足时的处理

当用户只说“做个页面”“帮我设计一下网页”“做得高级一点”，但缺少页面类型、风格方向、终端尺寸或素材策略时，先做一轮轻量澄清。优先补齐以下 4 项：

1. Web 页面目标
   - `品牌发布页（推荐）`
   - `信息说明页`
   - `运营活动页`
   - `Web 应用界面`
2. 视觉方向
   - `秩序发布（推荐）`
   - `编辑说明`
   - `轻科技`
   - `强视觉传播`
3. 终端与响应方向
   - `全端响应式（推荐）`
   - `桌面优先`
   - `移动优先`
   - `局部紧凑卡片`
4. 素材策略
   - `纯结构优先（推荐）`
   - `轻背景氛围`
   - `模块识别图标`
   - `结果证明预览`

如果用户说“你来定”或“不用问太多”，使用默认兜底：

- 页面目标：`品牌发布页`
- 视觉方向：`秩序发布`
- 终端方向：`全端响应式`
- 素材策略：`纯结构优先`

## 参考文件读取规则

- 默认先读 `references/aesthetic-system.md`
- 任务需要确定 Web 页面骨架时，读 `references/page-archetypes.md`
- 任务涉及交互、组件状态、操作反馈时，再读 `references/interaction-rules.md`
- 任务需要稳定输出结构化结果时，读 `references/output-contract.md`
- 交付前必须对照 `references/review-checklist.md`

## 输出结构

除非用户明确只要其中一部分，否则优先按 `references/output-contract.md` 的结构输出；至少覆盖下面 6 段：

### 1. 设计判断

- 当前任务属于哪种 Web 表达目标
- 推荐的视觉范式和原因
- 信息密度判断
- 是否需要素材，以及素材承担什么职责

### 2. 页面骨架

- 选中的 Web 页面原型
- 页面主命题（对应 `<h1>`）
- 一级区块顺序（对应 `<section>`）
- 每个区块承担的角色
- 需要删减或降级的内容
- 首屏折叠线（Above the fold）内的核心内容

### 3. 视觉规则

- 标题、摘要、卡片、辅助信息的层级关系
- 流式留白和节奏安排
- 配色与材质方向
- 图标、插图、截图、背景的职责边界

### 4. Web 交互规则

- `hover` / `active` / `focus` / `disabled` 状态定义
- 页面滚动或转场是否需要动效（需考虑性能影响）
- 哪些交互是主操作，哪些只能做弱提示
- 表单、标签、列表、卡片的交互强弱

### 5. Web 开发约束

- HTML 语义化建议（如 header, main, section, nav）
- 组件拆分建议
- CSS 样式 Token 建议（CSS Variables）
- 响应式策略（断点规则与流式布局建议）
- 哪些内容应做成数据驱动，哪些可静态实现

### 6. 交付自检

- 是否标题优先且符合视线流？
- 是否结构先于素材？
- 是否节奏清楚，具有呼吸感？
- 是否气质统一？
- 是否考虑了多端响应与流式适配？

## 实施规则

### 规则 1：先定范式，再定样式
不要一开始就从颜色、阴影和按钮圆角切入。先判断这是发布型、说明型、传播型还是应用界面，再决定布局、页面原型和细节。

### 规则 2：先让页面无图成立
如果页面去掉背景图、插图、截图就崩掉，说明结构没有成立。必须先保证纯结构版本可读。

### 规则 3：控制视觉权力中心
任何 Web 页面通常只能有一个第一中心。标题、主图、按钮、数据大字不能同时争抢第一注意力。

### 规则 4：小尺寸先删减，不先压缩
响应到移动端或紧凑卡片场景下，先裁掉次级模块、次级说明、重复标签，或者折叠次级导航，再考虑缩小字号或压缩间距。

### 规则 5：交互要服务结构
Web 交互动效和状态变化只能强化当前结构，不能打断阅读路径。弱内容不要做成强交互，次级按钮不要比主按钮更有存在感。

### 规则 6：尊重 Web 原生体验
不要破坏浏览器默认的滚动行为；确保所有可交互元素有明显的焦点状态（`focus-visible`）；文本应当是可被选中的，除非它是纯 UI 控件。

### 规则 7：首屏意识 (Above the fold)
确保核心命题和最关键的 Call-to-Action 按钮位于首屏视口内，不需要用户滚动即可看见和操作。

### 规则 8：先给骨架，再谈装饰
如果用户的原始需求混杂了配色、图标、插图、动效、卡片样式等细节，先收敛为页面原型和主次骨架，再补充视觉细节。

### 规则 9：解释为什么这样设计
如果输出代码、页面结构或规范说明，不只给结果，还要说明采用该范式、响应式布局和素材策略的原因，便于开发接手。

## 对代码生成的要求

当这个技能用于生成前端代码时：

- 必须优先使用语义化 HTML 标签（`<header>`, `<main>`, `<section>`, `<article>`, `<nav>`），而不是满篇 `<div>`。
- 优先使用现代 CSS 特性进行布局（Flexbox, Grid）和流式排版（`clamp()`, `rem`, `vh`/`vw`）。
- 视觉 Token 优先抽离为 CSS 变量（CSS Custom Properties），不要满屏硬编码颜色和间距。
- 响应式以结构变化为主（如 Grid 轨道的重构），结合媒体查询（Media Queries），而不是简单的 `transform: scale`。
- 确保基础的可访问性（A11y），如图标需要 `aria-label` 或 `alt`，颜色对比度达标。
- 先输出结构，再输出样式，再补交互状态，不要把所有差异都塞进一个大组件。
- 如用户未指定框架，优先遵循当前项目技术栈；如果是新建项目且无上下文，再结合宿主默认偏好。

## 标准输出模板

当用户让你“先给设计方案”或“先别写代码”，优先按下面模板输出：

```md
## 设计判断
- 页面目标：
- 视觉范式：
- 页面原型：
- 信息密度：
- 素材策略：

## 页面骨架（响应式）
- 主命题：
- 首屏可见项：
- 区块顺序：
- 每个区块的角色：
- 建议小屏删减项：

## 视觉规则
- 标题层级：
- 流式节奏与留白：
- 卡片/列表/图片区规则：
- 配色与材质方向：

## Web 交互规则
- 主操作与次操作：
- Hover/Focus 状态反馈：
- 动效建议：
- 原生体验注意事项：

## Web 开发约束
- 语义标签建议：
- 响应式断点与流式变化：
- 样式 Token（CSS Variables）：
- 建议组件拆分：

## 自检结论
- 当前最容易失控的点：
- 需要优先守住的原则：
```

## 页面原型速记

优先从下面几种 Web 页面原型中选择，而不是每次重新发明结构：

- `hero_release`：适合品牌发布页、能力介绍页、产品首屏
- `feature_grid`：适合功能介绍、能力矩阵、卖点分组
- `editorial_explain`：适合流程说明、规则导览、知识说明
- `dashboard_polish`：适合后台总览、数据概览、工作台整理
- `form_workspace`：适合录入、编辑、配置、审核类界面

## 交付前检查

交付前至少回答下面 8 个问题：

1. 第一眼看到的是不是页面主命题？核心按钮是否在首屏？
2. 去掉图片素材后，页面还能不能成立？
3. 是否存在两个以上一等重点在打架？
4. 模块之间是否有清晰的疏密节奏和合理的留白？
5. 小尺寸屏幕是否通过删减与流式布局，而不是粗暴挤压完成适配？
6. 结构是否符合 HTML 语义化？是否保留了 Focus 等原生交互状态？
7. 交互反馈是否强化了结构，而不是制造噪声？
8. 整体是否像同一套 Web 设计系统产物，而不是随意拼装的网页？
